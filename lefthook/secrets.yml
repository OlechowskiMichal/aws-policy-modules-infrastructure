pre-commit:
  commands:
    gitleaks:
      run: task hooks:gitleaks:protect
      fail_text: "gitleaks detected secrets in staged changes"

pre-push:
  commands:
    gitleaks:
      run: task hooks:gitleaks:detect
      fail_text: "gitleaks detected secrets in repository"
